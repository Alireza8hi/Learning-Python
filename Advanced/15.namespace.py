# objects(variable, method, function, class, ...):
# 1 built-in: we access them in all files       example: print(), __name__, type(), ...
# 2 global: we access them in one file that defined in it    example: x
# 3 local: we access them in one scope that defined in it    example: y


x = 20


def test():
    y = 25
