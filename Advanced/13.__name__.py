# __name__: a special variable that assign by default
# if this file is main file, __name__  = __main__
# but if this file isn't main file(is import file) __name__ = our file's name

print(__name__)

import my_file
